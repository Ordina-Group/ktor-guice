
# Routing registration issues

## 1) Wrapping route into an authenticated route
Currently, there is no way of wrapping existing routes into a authenticated route. 
This means authentication is currently not working